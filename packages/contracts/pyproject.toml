[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "contracts"
version = "0.1.0"
description = "Schemas and contracts for the platform"
requires-python = ">=3.8"

[project.optional-dependencies]
dev = ["pytest", "ruff"]